@model IEnumerable<WebApplication16.Models.Xiaomi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.imageName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.XiaomiAciklama)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fiyat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <img src="~/image/Xiaomi/@item.imageName" asp-append-version="true" width="50" height="50" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.XiaomiAciklama)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fiyat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.XiaomiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.XiaomiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.XiaomiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
