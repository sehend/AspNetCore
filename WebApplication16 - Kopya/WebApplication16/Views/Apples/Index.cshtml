@model IEnumerable<WebApplication16.Models.Apple>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.imageName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppleAciklama)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fiyat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <img src="~/image/Apple/@item.imageName" asp-append-version="true" width="50" height="50" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppleAciklama)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fiyat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AppleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AppleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AppleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
